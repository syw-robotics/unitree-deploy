- web_demo现在是为veloity tracking的policy定制的，如果用户的 command有变化，根据自己的policy定制，那么当前的代码结构应该怎 么去适应的，我们宗旨是尽量不要去碰src中的核心代码，you only write code once


• 不能把当前 unitree-web-demo 直接部署到 GitHub Pages 上作为可运行 demo。

  原因：GitHub Pages 只能托管静态文件，比如 HTML/CSS/JS。当前 demo 需要 Python 后端运行：

  - MuJoCo simulation
  - ONNX policy inference
  - /api/scene
  - /api/frame
  - /api/command
  - /api/drag

  所以 Pages 不能执行 unitree-web-demo --robot g1 --ckpt ...。

  推荐方案分三种：

  方案 A：GitHub Pages 只做项目主页
  最简单、最稳：

  - GitHub Pages 放 README、截图、视频、启动命令。
  - 用户本地运行：

  unitree-web-demo --robot g1 --ckpt ckpt/g1/vanilla_ppo_flat

  这适合作为开源项目主页。

  方案 B：Pages 前端 + 云端 Python 后端
  如果你想真正在线交互：

  - GitHub Pages 托管 Three.js 前端。
  - 后端部署到 VPS / Hugging Face Spaces / Render / Fly.io / 自己服务器。
  - 前端请求后端 API，例如：

  const API_BASE = "https://your-demo-backend.example.com";
  fetch(`${API_BASE}/api/scene`);

  后端需要加 CORS，并且不要允许用户传任意 --ckpt 路径，应该固定加载某个公开 demo policy。

  方案 C：完全静态浏览器 demo
  理论上可以，但工作量大：

  - MuJoCo WebAssembly
  - ONNX Runtime Web
  - policy、MJCF、mesh 全部放浏览器
  - 前端直接跑仿真和推理

  这会变成另一个项目，不建议现在走。

  如果只是“推到 GitHub 并开 Pages”，流程大概是：

  git remote add origin git@github.com:<user>/<repo>.git
  git push -u origin dev/syw

  然后在 GitHub 仓库里启用 Pages，选择静态主页目录或分支。

  但要注意：这样只能展示页面/文档，不能运行当前 Python 后端。当前真正可运行的 online demo，需要额外部署 backend。
